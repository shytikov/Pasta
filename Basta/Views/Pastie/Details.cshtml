@model Basta.Models.Pastie

@{
    ViewBag.Title = "#" + Model.Id;
    ViewBag.Highlight = true;
    Layout = "~/Views/Layout.cshtml";
}
            <h1>#@Model.Id</h1>

            <pre><code class="prettyprint linenums">@Html.DisplayFor(model => model.Content)</code></pre>