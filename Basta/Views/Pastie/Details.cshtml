@model Basta.Models.Pastie

@{
    ViewBag.Title = "#" + Model.Id;
    Layout = "~/Views/Layout.cshtml";
}
            <h1>#@Model.Id</h1>

            <pre><code class="prettyprint">@Html.DisplayFor(model => model.Content)</code></pre>
